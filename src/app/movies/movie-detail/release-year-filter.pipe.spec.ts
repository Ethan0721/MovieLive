import { ReleaseYearFilterPipe } from './release-year-filter.pipe';

describe('ReleaseYearFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseYearFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
